{
  function addOne(num: number) {
    return num + 1;
  }

  const res = addOne(3);
  console.log(res);
  console.log();
}
{
  const multiple = (x: number, y: number) => {
    return x * y;
  };

  const res = multiple(2, 10);
  console.log(res);
}
