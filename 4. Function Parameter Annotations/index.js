"use strict";
{
    function addOne(num) {
        return num + 1;
    }
    const res = addOne(3);
    console.log(res);
    console.log();
}
{
    const multiple = (x, y) => {
        return x * y;
    };
    const res = multiple(2, 10);
    console.log(res);
}
