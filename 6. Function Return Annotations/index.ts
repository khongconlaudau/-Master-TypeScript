{
  function divide(x: number, y: number): number {
    return x / y;
  }
  const rs = divide(10, 2);
  console.log(rs);
  console.log();
}

{
  const sum = (num1: number, num2: number): number => {
    return num1 + num2;
  };

  const rs = sum(10,5)
  console.log(rs);
  
}
