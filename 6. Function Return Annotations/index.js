"use strict";
{
    function divide(x, y) {
        return x / y;
    }
    const rs = divide(10, 2);
    console.log(rs);
    console.log();
}
{
    const sum = (num1, num2) => {
        return num1 + num2;
    };
    const rs = sum(10, 5);
    console.log(rs);
}
